#!/usr/bin/env python3

import os.system

choice = int(input("Enter 1 if you want to be prompted for input, or 2 to run from the CSV File: "))

if choice == 1:
    system(./openstackrc_maker.py)
else:
    isystem(./rclooper.py)


